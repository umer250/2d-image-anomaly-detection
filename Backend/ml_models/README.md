# ML Models Directory

Place your trained model files here.

Recommended formats:
- `.h5` (Keras/TensorFlow)
- `.pt` / `.pth` (PyTorch)
- `.onnx` (ONNX)

Update `MODEL_PATH` in `.env` to point to your specific model file.
